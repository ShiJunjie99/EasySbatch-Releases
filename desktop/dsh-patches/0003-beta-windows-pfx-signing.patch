diff --git a/apps/desktop/electron-builder.config.mjs b/apps/desktop/electron-builder.config.mjs
--- a/apps/desktop/electron-builder.config.mjs
+++ b/apps/desktop/electron-builder.config.mjs
@@ -33,7 +33,8 @@ export function createElectronBuilderConfig(
   const packagesWindows = targetPlatform === 'win32'
   const macOSSigning = packagesMacOS && !unsigned ? resolveMacOSSigningEnvironment(env) : undefined
   if (packagesMacOS && !unsigned) resolveMacOSNotarizationEnvironment(env)
-  const windowsSigner = packagesWindows && !unsigned
+  const safeNetSigning = env.BETA_EASYSBATCH_WINDOWS_SIGNING_MODE === 'safenet'
+  const windowsSigner = packagesWindows && !unsigned && safeNetSigning
     ? createWindowsTokenSigner({
         certificateFile: env.DSH_DESKTOP_WINDOWS_CER_FILE,
         signTool: env.DSH_DESKTOP_WINDOWS_SIGNTOOL,
@@ -46,6 +47,10 @@ export function createElectronBuilderConfig(
   if (windowsSigner !== undefined) {
     installWindowsNsisBootstrapSigner({ sign: windowsSigner })
   }
+  if (packagesWindows && !unsigned && !safeNetSigning
+    && (!env.WIN_CSC_LINK?.trim() || !env.WIN_CSC_KEY_PASSWORD)) {
+    throw new Error('Beta EasySbatch signed Windows packaging requires WIN_CSC_LINK and WIN_CSC_KEY_PASSWORD')
+  }
   const update = resolveDesktopAutoUpdateConfig(unsigned ? {
     ...env,
     DSH_DESKTOP_AUTO_UPDATE_ENV: 'test',
@@ -97,7 +103,7 @@ export function createElectronBuilderConfig(
     win: {
       forceCodeSigning: !unsigned,
       signtoolOptions: {
-        sign: windowsSigner,
+        ...(windowsSigner === undefined ? {} : { sign: windowsSigner }),
         signingHashAlgorithms: ['sha256'],
       },
       target: ['nsis'],
diff --git a/apps/desktop/scripts/package-target.ts b/apps/desktop/scripts/package-target.ts
--- a/apps/desktop/scripts/package-target.ts
+++ b/apps/desktop/scripts/package-target.ts
@@ -17,7 +17,10 @@ const WINDOWS_SIGNING_ENV_NAMES = [
   'DSH_DESKTOP_WINDOWS_KEY_CONTAINER',
   'DSH_DESKTOP_WINDOWS_SIGNTOOL',
   'DSH_DESKTOP_WINDOWS_TOKEN_PIN',
+  'WIN_CSC_KEY_PASSWORD',
+  'WIN_CSC_LINK',
 ] as const
+const WINDOWS_SIGNING_ENV_NAME_SET = new Set<string>(WINDOWS_SIGNING_ENV_NAMES)
 const DESKTOP_UPLOAD_CREDENTIAL_ENV_NAMES = new Set([
   'DOWNLOAD_TEST_COS_SECRET_ID',
   'DOWNLOAD_TEST_COS_SECRET_KEY',
@@ -66,3 +69,4 @@ export function withoutWindowsSigningEnvironment(environment: NodeJS.ProcessEnv): NodeJS.ProcessEnv {
   return Object.fromEntries(Object.entries(environment)
-    .filter(([name]) => !name.startsWith(WINDOWS_SIGNING_ENV_PREFIX)))
+    .filter(([name]) => !name.startsWith(WINDOWS_SIGNING_ENV_PREFIX)
+      && !WINDOWS_SIGNING_ENV_NAME_SET.has(name)))
 }
