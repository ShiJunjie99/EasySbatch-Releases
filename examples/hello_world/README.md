# Hello World

这是一个只输出 `hello world` 的 Python 示例，无第三方依赖。

在本目录运行：

```bash
python hello.py
```

测试网页分析时，将 Project directory 填为这个文件夹的绝对路径，
点击 Scan Project，再输入任务描述“运行 hello.py，输出 hello world”并 Analyze with AI。
分析只产生草稿，不会执行这个程序或提交作业。
